# Dépendances de production — Streamlit Cloud
# Contraintes de version : >= version testée, < prochaine major breaking change

streamlit>=1.35,<2
polars>=0.20,<2
# Pinned < 2.10 : storage3 >= 2.x tire pyiceberg qui nécessite Visual C++ (pas de wheel Python 3.14)
supabase>=2.4,<2.10
plotly>=5.20,<6
stripe>=8.0,<10
requests>=2.31,<3
python-dotenv>=1.0,<2
streamlit-cookies-manager>=0.2,<0.3
